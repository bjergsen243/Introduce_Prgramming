#include <stdio.h>
int main()
{
    int a, b;
    // printf("Nhap 2 so a, b: ");
    scanf("%d %d", &a, &b);
    int c = a + b;
    // printf("Tong 2 so a va b la: %d", c);
    printf("%d", c);
    return 0;
}
