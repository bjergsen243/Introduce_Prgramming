#include <stdio.h>
int main()
{
    char s1[50], s2[50], s3[50];
    //printf("Nhap ten cua 3 nguoi: ");
    scanf("%s %s %s", s1, s2, s3);
    printf("Hi %s, %s and %s.", s3, s2, s1);
    return 0;
}
